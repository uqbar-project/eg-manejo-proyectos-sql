name: build-auth

on: 
  push

jobs:
  gradle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Start SQL
        run: docker-compose -f "docker-compose.yml" up -d --build
        
